# webcam_ai_item_recognition_test
I wanted to explore coding with webcam hardware and ai vision models.  So I started this project in March 2026 to learn more about the different pythons packages to achieve this:  ultralytics and  cv2.

## 🚀 Getting Started

### Prerequisites
You will need Python 3.x and pip installed on your machine. We recommend using a virtual environment to manage dependencies.

### Installation

1. Clone the repository:
   ```bash
   git clone [https://github.com/your-username/your-repo-name.git](https://github.com/your-username/your-repo-name.git)
   cd your-repo-name

2. Create a virtual environment:
 

#### On Windows
  
   ```bash
  python -m venv venv
  venv\Scripts\activate
   ```

#### On macOS/Linux
 ```
python3 -m venv venv
source venv/bin/activate
 ```

3. Install dependencies:

 ```bash
pip install -r requirements.txt
 ```
Download YOLO Weights:
(Include a brief note here on where to get the yolov8n.pt or relevant model weights, or if your code handles the auto-download, mention that!)

Usage
Run the main detection script using the following command:

 ```bash
python main.py --source /path/to/your/image_or_video.mp4
 ```
